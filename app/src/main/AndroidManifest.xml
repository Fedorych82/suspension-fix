<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature
        android:name="android.hardware.type.automotive"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <!-- Вендорная публичная либа CAN-шины: обязательна для targetSdk >= 31,
             чтобы линкер пустил её в namespace приложения. -->
        <uses-native-library
            android:name="libqg_hal.so"
            android:required="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".SuspensionReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.suspension.fix.LOCK_SUSPENSION" />
                <action android:name="com.suspension.fix.UNLOCK_SUSPENSION" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
